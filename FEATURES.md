# Feature Tracker — Dogboxx

> Priority: **P1** = must-have, **P2** = should-have, **P3** = nice-to-have
> Effort: **S** = small (< 1hr), **M** = medium (1-4hrs), **L** = large (4hrs+)
> Status: 🔲 todo · 🔧 in progress · ✅ done · ❌ dropped

## Core Booking Flow

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 1 | P1 | L | ✅ | **Booking workflow with capacity checks** | Walker availability × max_per_walker. Waitlist when full. Auto-assigns least-loaded walker on creation — confirms immediately if capacity available. |
| 2 | P1 | M | ✅ | **Recurring bookings (client)** | Start/end date + frequency (daily/weekly). Server expands to individual bookings. Skips weekends, duplicates. |
| 3 | P1 | M | ✅ | **Admin booking board** | Calendar + slot view. Confirm/cancel requests. Drag-to-reorder pickup order per walker. |
| 4 | P1 | M | ✅ | **Admin dogs view** | Searchable table of all dogs. Book on owner's behalf (one-off or recurring) via modal. Same pending flow as client-initiated. |
| 5 | P1 | M | ✅ | **Prevent duplicate bookings** | Block same dog+date+slot. Max 2 bookings per dog per day (one per slot). DB partial unique index. |
| 6 | P1 | M | ✅ | **Password reset** | "Forgot password?" → token email → set new password. Resend API, itsdangerous token (1hr expiry, invalidated on use). |
| 7 | P1 | M | ❌ | **Booking capacity display for clients** | Dropped — "available" indicator in UI is sufficient. Exact slot counts not needed. |
| 8 | P2 | L | ✅ | **"Book both walks" option (client)** | Checkbox books AM + PM in one action via /client/book_both. Admin board shows layered-icon modifier pill. |

## Walker

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 10 | P1 | M | ✅ | **Walker pickup list** | Daily route view with dog photo, owner, address, pickup instructions, ordered by pickup_order. Date navigation. |
| 11 | P2 | M | ✅ | **Walker schedule management** | Default weekly schedule (day + slot). Admin sets. Walker can view. |
| 12 | P2 | M | ✅ | **Walker unavailability** | Date-specific exceptions (per slot). Admin marks unavailability. Reduces capacity for that slot automatically. |
| 13 | P3 | L | ✅ | **Walker self-manage availability** | Walkers flag their own exceptions (holidays, sick days) rather than admin doing it. |
| 14 | P3 | S | ✅ | **Google Maps pickup directions** | Maps button on each pickup card in /walker/pickups. |
| 15 | P2 | M | ✅ | **Walker ad hoc available days** | Walkers can add one-off available days outside their default schedule. Inverse of the existing unavailability model. New `walker_adhoc_availability` table (or reuse existing with a flag). Capacity checks need updating to include these. |
| 16 | P2 | M | ✅ | **Admin override walker unavailability on allocation board** | Admin can assign dogs to a slot even if walker has marked themselves unavailable. Override shown visually (warning state). Booking creation bypasses the unavailability block when admin-initiated. |

## Admin

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 20 | P1 | M | ✅ | **Client management** | Create, view, edit client accounts. Notification audit trail per client. |
| 21 | P1 | M | ✅ | **Walker management** | Create walkers. Set/edit default schedule. Mark unavailability. |
| 22 | P2 | S | ✅ | **Admin is also a walker** | is_admin flag on User. Admin can be a walker. "My Pickup List" in admin sidebar. |
| 23 | P2 | M | ✅ | **Admin dashboard stats** | Stat cards (pending, clients, dogs, walkers), 4-week booking chart by slot+status, walker availability grid. Revenue on /admin/revenue. |
| 24 | P3 | L | 🔲 | **Dental cleans service type** | Admin: manage available date+time slots. Client: book from available slots. Nav stub removed — add back when built. Premature `dental_confirmed`/`dental_available` notification types were removed (2026-05-29) — re-add when built. |
| 25 | P3 | L | ❌ | **Invoicing (standalone)** | Superseded by #26. |
| 26 | P2 | L | ✅ | **Invoicing view (admin)** | /admin/invoicing: monthly summary per client. /admin/invoicing/<id>: line items + weekly breakdown. Billable cancels (<5 days notice), double-slot discount, drop-in pricing. PricingConfig history. Admin late-cancel billing override (PR #134): `Booking.bill_cancellation` (bill/waive/default) — admin cancels inside the notice window bill by default, waivable per-cancellation from the `/admin/dogs` cancel modal. |
| 27 | P2 | L | ✅ | **Multiple clients per dog** | dog_owners join table with primary/secondary roles. Admin join/revoke modal. Secondary owners can book and view shared dogs. |
| 29 | P2 | L | ✅ | **Drop-in service type** | Client books AM/PM drop-in visits. Admin drop-in board (assign walkers, confirm/cancel, reorder). Walker pickup list includes drop-ins. Invoicing tracks drop-ins separately at price_per_drop_in. does_drop_ins flag on walkers. |
| 28 | P3 | M | ✅ | **CSV client/dog import** | Upload CSV matching the create-client form fields. Bulk create client + dog records. Validation with error report on bad rows. No need to handle joined accounts — those are done manually post-import. |
| 35 | P2 | M | ✅ | **Admin bulk booking operations** | Bulk-cancel upcoming bookings for a dog from `/admin/dogs` view (with preview modal showing dates). Service filter on the cancel modal (All / Walk / Drop In) — cancel one service without touching others. Recurring create on admin's behalf from the same view. Day-of-week filter on pause-walks. |
| 36 | P2 | M | ✅ | **Closures** | Admin marks a date (or date range, #169) as closed — auto-cancels active bookings with client notifications. Prevents new bookings on closed dates. Preview endpoint before confirming, warns of already-closed dates within a range and skips them. Multi-date closures share a `range_id`; admin list and activity feed collapse them into one entry. |
| 37 | P3 | S | ✅ | **Daily walker messages** | Admin-authored announcements shown to walkers on the pickup list for a given date. Auto-purge of old messages. |
| 38 | P2 | L | ✅ | **Admin broadcasts** | One-shot message to all clients booked on a chosen date + slot scope (all / morning / afternoon). Bell + email delivery. Recipients resolved at send time from confirmed bookings (primary + secondary co-owners). Broadcast bar shown on assignment board. Audit rows kept. PRs #99–102. |

## Client

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 30 | P1 | M | ✅ | **Client onboarding** | Manual address entry + optional Google Maps pin URL (Places autocomplete removed), pickup instructions, dog profile. |
| 31 | P1 | M | ✅ | **Client profile edit** | Edit address, notification prefs, dog details + photo. |
| 32 | P2 | M | ✅ | **Monthly walk summary** | Client-facing summary of walks taken and any outstanding items. |
| 34 | P3 | M | ✅ | **Multi-dog support (admin)** | Admin can add a second (or subsequent) primary dog to a client via modal on the client detail page. Data model and booking flow already supported multiple dogs. Client self-service out of scope. |

## Newsletter

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 60 | P2 | L | ✅ | **Client newsletter (admin)** | `/admin/newsletter` — Quill WYSIWYG, merge tags ({{firstname}}, {{dog_name}}), recipient sidebar, test send to lydia@dogboxx.org, confirm modal. Resend batch API. |
| 61 | P2 | S | ✅ | **Email marketing opt-out** | `email_marketing` boolean on User (default True). One-click `/auth/unsubscribe/<token>` link in every newsletter. GDPR compliant. |

## Notifications

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 40 | P1 | L | ✅ | **In-app notification system** | Bell icon, unread count, mark read. Persistent DB records. |
| 41 | P1 | S | ✅ | **Notify client: booking confirmed** | Triggered when admin confirms a booking. |
| 42 | P1 | S | ✅ | **Notify admin: booking requested** | Triggered when client submits a new booking. |
| 43 | P1 | S | ✅ | **Notify client: booking cancelled** | Triggered when admin cancels a booking. |
| 44 | P1 | S | ✅ | **Notify walker: assigned to booking** | Triggered when admin assigns a walker. |
| 45 | P1 | S | ✅ | **Notification audit trail (admin)** | Admin can see notification history per client on their detail page. |
| 46 | P1 | L | ✅ | **Notification system overhaul** | `BookingStatusChange` append-only audit log at every status transition (chokepoint in `app/utils/booking_status.py`). `NotificationBatch` + `summarise()` for grouped bulk notifications. Caps: DB=100, page=50, bell=5. `batch_id` correlates rows from one bulk action. PRs #114–121. |
| 47 | P2 | L | ✅ | **Admin activity feed** | `/admin/activity` rebuilt from `BookingStatusChange` log — slot moves, booking-reset events, bulk-action clustering. Collapsible batch groups. PR #118 (Session 4). |
| 48 | P3 | L | ✅ | **Web Push / PWA push notifications** | VAPID-signed push via `pywebpush`. iOS PWA service worker (`app/static/js/sw.js`). `PushSubscription` model stores endpoints per user/device. Bell notification triggers push on unread. Home-screen badge reconciled to server truth on every page load + `visibilitychange` (PRs #128/#129). The bell dropdown list also reconciles on `visibilitychange` via `GET /notifications/recent` (count + top-N) so it self-heals after iOS suspends the EventSource (PR #135). SSE uses Redis pub/sub when `REDIS_URL` is set so cross-worker events don't drop. |

## Infrastructure & Quality

| # | Priority | Effort | Status | Feature | Notes |
|---|----------|--------|--------|---------|-------|
| 50 | P1 | M | ✅ | **PostgreSQL migration** | Moved from SQLite to PostgreSQL. Flask-Migrate (Alembic) for schema management. |
| 51 | P1 | M | ✅ | **Security hardening** | CSRF, rate limiting, CSP headers, secure cookies, UUID file uploads, session hardening. June 2026 pentest follow-up (PR #141): concurrent-duplicate booking → graceful 409, login user-enumeration timing oracle closed (dummy-hash), Web Push endpoint SSRF allowlist (`is_allowed_push_endpoint`, currently log-only — enforce-400 flip pending). |
| 52 | P1 | S | ✅ | **DB indexes** | Indexes on date, walker_id, user_id, dog_id, status for query performance. |
| 53 | P1 | M | ✅ | **Git branching** | `develop` for ongoing work, `main` for production. PRs required to merge to main. |
| 54 | P1 | L | ✅ | **Unit test suite** | 522 tests across auth, bookings, capacity, multi-owner, notifications, drop-in, invoicing, pricing, activity feed, broadcasts, closures, walker schedule, bulk-cancel, SSE transport, rich-text sanitization, pickup notes. All passing on Postgres CI. |
| 55 | P2 | M | ✅ | **Password reset flow** | Email-based token reset via Resend. noreply@dogboxx.org verified. RESEND_API_KEY + APP_BASE_URL needed in prod env. |
| 56 | P3 | S | ✅ | **CI/CD pipeline** | GitHub Actions (test.yml): runs pytest on push to main/develop and all PRs. All runs green. |
| 57 | P3 | M | ✅ | **PWA service worker** | iOS home-screen install + Android PWA support. Pre-cached assets, pull-to-refresh (shared IIFE in both layouts), standalone-mode detection (`navigator.standalone \|\| display-mode:standalone`). |
| 58 | P3 | M | 📋 | **QuickBooks Online export** | Scoped only, not started. Feed monthly invoices into QBO. Two paths: CSV export (~half a day, no deps, recommended Phase 1) vs QBO API push (OAuth + token lifecycle, only if button/payment-sync needed). Both consume the existing `invoice_for_client` dict. Full write-up + open decisions in the private handbook (`docs_internal/docs/reference/quickbooks-scoping.md`; `docs/QUICKBOOKS_INTEGRATION.md` retired 2026-07-12, full text in git history). |
| 59 | P2 | L | ✅ | **July 2026 code-review remediation** | **Complete.** All 16 findings closed: #1–12, #15–16 deployed to prod across PRs #143–#152 (last: #12 client/routes.py→views/ split, 2026-07-05); #13 (tz-aware DateTime, new cols only) and #14 (recurring-booking lock, scale note) are deliberate no-action guidance. Verification pass 2026-07-05 confirmed all items implemented correctly and caught 3 `logging.error` stragglers the #10 sweep missed (fixed, `cdfdc9a`). Findings absorbed into the private handbook's Gotchas Index (`docs_internal/docs/reference/gotchas.md`); full text in git history (file retired 2026-07-12). |
| 62 | P2 | M | ✅ | **Production monitoring — Sentry + uptime check** | From the July 2026 big-picture review: prod 500s are only visible by tailing `railway logs` (tracebacks are logged since review #10, but nobody is *notified*), and nothing watches the app between deploys. (a) **Sentry** — `sentry_sdk.init()` guarded on `SENTRY_DSN` env var (unset in dev/CI/test, so no local noise); `send_default_pii=False` **and** `include_local_variables=False` — both needed, they're independent flags, and without the second, stack-frame locals (e.g. the plaintext `password` local in `auth/routes.py::login()`) would ship to Sentry on any downstream exception; `traces_sample_rate=1.0`; default `LoggingIntegration` left on, so every `logging.error`/`logging.exception` call becomes a Sentry event too, not just unhandled 500s — deliberate choice for max coverage, revisit if specific handled-error call sites turn out noisy. Deployed to prod via PR #153 (2026-07-06), verified end-to-end with a real test event tagged `environment: production`. (b) **Uptime check** — used Sentry's own built-in Uptime Monitoring instead of the originally-scoped UptimeRobot (one less third-party account, alerts land alongside error alerts). `GET https://app.dogboxx.org/health`, every 5 min, 5s timeout — 5 min chosen deliberately: `/health` isn't exempt from the global rate limiter (`50/hour` per IP, the `default_limits_exempt_when` exemption only covers authenticated users), so a 1-min interval (60/hour) would trip 429s and read as false-positive downtime; 5 min (12/hour) stays comfortably under the cap. |
| 63 | P2 | M | ✅ | **Backup confidence — restore drill + R2 photo reconciliation** | Backups are assumed, not proven — and auditing this surfaced a real incident (see #63a note). (a) **Postgres restore drill — done 2026-07-11, plan changed along the way.** Investigating this found the nightly `pg_dump` → R2 GitHub Actions backup had been silently producing empty (20-byte) dumps for ~3 months (pg_dump v16 vs. Railway's Postgres v18, no `pipefail`) — fixed directly (`.github/workflows/backup.yml`, PR #154, verified with a real 856 KB dump). Original plan was Railway PITR (separate sibling service, never touches the live volume) but PITR needs the Pro plan — declined, Hobby-tier cost jump wasn't worth it given the now-fixed R2 dump already gives real coverage. Rescoped drill instead: spun up a fully separate throwaway Railway project (`dogboxx-restore-drill`) with its own bare Postgres service, downloaded the latest verified dump (`2026-07-10.sql.gz`, 886 KB) from `dogboxx-db-backups`, and `psql`-restored it there — isolated from prod at every step (never touched the live DB, never relinked the local CLI away from prod). Verified clean: 0 errors, all 19 tables present, sane row counts (65 users / 59 clients / 48 dogs / 3,901 bookings / 1,810 status-change rows / 8 walkers), restored `alembic_version` (`8763c31f1b76`) matches the repo's current migration head exactly, `flask db upgrade` against it reported already-at-head, and spot-checked rows contained real recognizable data. The Postgres service + volume were deleted afterward but the project + environment are kept around empty for reuse next time — full step-by-step runbook now in the private handbook (`docs/RESTORE_DRILL.md` retired 2026-07-12; full text in git history). (b) **R2 photo reconciliation — shipped, PR #155.** `/api/internal/uploads-manifest` (`app/blueprints/api/routes.py`, gated by `internal_only`/`INTERNAL_API_SECRET` — `app/utils/decorators.py`) lets a sibling service read the uploads volume, since Railway volumes can only mount to one service; `flask reconcile-uploads` (`run.py` + `app/utils/uploads_reconcile.py`) fetches that manifest plus the R2 bucket listing and diffs them, emailing `BUG_REPORTS` only if something's actually missing or size-mismatched. Manually verified end-to-end against the real volume/bucket before writing any code (23/23 dog photos + 9/9 profile photos matched exactly). Running as a dedicated Railway cron service (`reconcile-uploads`, weekly Monday 3am UTC). (c) **Full disaster-recovery checklist — documented, not drilled.** Covers rebuilding every Railway service from nothing (not just the DB) — service recreation order, cron config-as-code gotchas, env vars (now backed up in 1Password), DNS re-point (provider not yet identified — flagged as a gap). Unlike (a), this hasn't been rehearsed end-to-end; it's written from the current config, not from having actually done it. Full checklist now in the private handbook (`docs/DISASTER_RECOVERY.md` retired 2026-07-12; full text in git history). |
| 64 | P3 | S | ✅ | **Expired-session cleanup via Railway cron** | The prod `sessions` table grows forever (~4.8k rows at July 2026) — Flask-Session 0.8.0 never deletes expired rows unless told to. Chosen approach: Railway cron service running `flask session_cleanup` (the command Flask-Session auto-registers **when `SESSION_CLEANUP_N_REQUESTS` is unset** — the two modes are mutually exclusive, so don't also set that config or the command disappears). Deliberately preferred over the in-request `SESSION_CLEANUP_N_REQUESTS=N` one-liner as a learning exercise for Railway cron — the one-liner remains the documented fallback (tail-latency + shared-`db.session` commit caveats discussed 2026-07-06). **Live in prod** — new `session-cleanup` Railway service, `startCommand = flask session_cleanup` sourced from a dedicated config-as-code file (`railway.session-cleanup.toml`, PR #156) rather than the root `railway.toml`, same reasoning as `reconcile-uploads` (#63b): a same-repo service with no custom config path silently inherits the root file's `bash scripts/start.sh` + `/health` healthcheck, which a cron job can never satisfy. Vars are `${{web.*}}` references to `SECRET_KEY`/`DATABASE_URL`/`FLASK_ENV`; cron schedule `0 4 * * *`. Verified two ways: `flask session_cleanup` run directly via `railway ssh` against `web` (exit 0), and the deployed service's own resolved config confirmed via its deployment detail (not the service-level view, which misleadingly read empty) — `configFile`/`fileServiceManifest` show `startCommand: flask session_cleanup`, `healthcheckPath: null`. |
| 65 | P2 | L | ✅ | **Rich-text pickup notes + photo, formatted broadcasts** | Pickup notes (`Dog.pickup_instructions`, now `Text`) support Quill formatting (headings, bold/italic/underline, lists, blockquote, link) across every edit surface — client `/profile`, onboarding, admin client form/detail, admin dogs table — plus an optional reference photo (`Dog.pickup_notes_photo`, uploadable client or admin side, `uploads/pickup_notes/` subfolder). Broadcasts get the same Quill composer; the bell notification gets a tags-stripped plain-text summary (`rich_text_to_plain`) since it can't render markup, email/DB audit row keep full HTML. Also closed a pre-existing gap where the newsletter's Quill HTML went to email completely unsanitized. All four rich-text surfaces (Daily Messages, pickup notes, broadcasts, newsletter) now share one bleach allowlist + scoped CSS sanitizer (`app/utils/sanitize.py`, preserves the newsletter's color picker). Migration `15f7f5bab746`. PR #159 (2026-07-19), merged to develop then split into PR #161 (feature) → main alongside PR #160 (unrelated watchdog dev-logging fix) — deployed to prod 2026-07-19, migration ran clean on Railway. Two pre-existing bugs found and fixed along the way (admin_client_detail.html rendering literal "None" for dogs with no pickup notes; the walker pickup-list modal inheriting centered text from the page layout default) plus a real gap in the admin dogs table's edit modal (missing photo upload, and a dead `.edit-dog-btn` selector that silently no-opped the post-save refresh — replaced with reload-on-save). 434 tests passing. |
| 60 | P4 | S | ✅ | **Dead-local cleanup found during #12 split** | Two pre-existing (not introduced by the split) unused-variable nits caught by pyflakes while splitting `client/routes.py`: `views/bookings.py::recurring_booking` computed `freq_label`/`slot_label`/`service_label` that were never read (notification text goes through `NotificationBatch`/`summarise()` instead); `views/profile.py::update_pickup` fetched `client = Client.query...` and never used it (pickup notes live on `Dog`, not `Client`). Both removed directly on develop. 392 tests pass. |
| 66 | P2 | M | ✅ | **Weekly Overview — walker week tab + admin roster/copy tool** | Walker gets a third tab ("My pickups \| Daily overview \| Weekly overview") extending the existing daily overview to a full week; admin gets a new left-nav page (`/admin/weekly-overview`, under Rosters) listing every walker with a booking that week plus a **Copy** button that puts a plain-text WhatsApp/email-ready summary of that walker's week on the clipboard. "Scheduled to work" = actual bookings/dogs assigned (same data source as the daily overview), not raw availability. **DogBoxx only operates Monday-Friday** — every surface here (week view, roster list, copy text) shows exactly 5 days; Saturday/Sunday never render, even though the shared data layer computes a Mon-Fri range internally rather than a generic 7-day week. New shared module `app/utils/weekly_schedule.py` (get_week_start, fetch_week_bookings, group_bookings_by_slot, build_week_by_day, build_week_by_walker, format_walker_week_text, day_slot_parts) is the single source of truth both the walker route (`app/blueprints/walker/routes.py::weekly_overview`) and the admin view (`app/blueprints/admin/views/weekly_overview.py`) call into, so the on-screen roster and the clipboard text can't drift apart; `_build_daily_overview` was refactored to delegate to the same `group_bookings_by_slot` helper (behavior-preserving). Walker week nav is prev/next-week links (no calendar widget, no AJAX) — matches `walker_monthly_summary.html`'s existing pattern rather than the daily/pickup pages' calendar+partial-fetch approach, since a week view doesn't need day-level picking. Clipboard copy is a small event-delegated handler inline in `admin_layout.html` (`.copy-week-btn` + `data-copy-text`, reuses `showToast()`) rather than a new static JS file, sidestepping the service-worker `CACHE_VERSION` question entirely. No schema changes. 34 new tests (`tests/test_weekly_schedule_util.py`, `tests/test_walker_weekly_overview.py`, `tests/test_admin_weekly_overview.py`, plus `tests/test_walker_daily_overview.py` closing a pre-existing coverage gap on the daily overview page this refactor touched); 468 total passing. **Follow-up (PR #167, 2026-07-26):** walker weekly overview gets its own top-of-page "who's working, by day" card (same `day_walker_names()` helper as the admin one), highlighting the viewing walker's own working days with the page's existing pink-accent/filled-calendar convention instead of listing every walker equally. Separately, `/walker/pickups` (the walker login landing page) now defaults to the coming Monday when opened with no explicit date on a Saturday/Sunday, rather than a closed day with nothing to show — explicit `?date=`/path-param navigation to a weekend date is unaffected. No schema changes; 4 new tests (`tests/test_walker_pickups_weekend_default.py`), 2 pre-existing tests updated (`test_pickup_notes.py`, `test_walker_daily_overview.py`) that had coupled a booking on `date.today()` to the old always-today default; 482 total passing. Deployed to prod 2026-07-26, Railway logs clean. |

---

## Dropped / Descoped

| Feature | Reason |
|---|---|
| Firebase Auth migration | Overkill for current scale. Flask-Login is sufficient. Revisit post-launch. |
| Public client self-registration | Business prefers admin-created accounts (vets clients first). Register route + `RegisterForm` + `register.html` removed entirely in PR #124 (2026-06-05); `/onboard` remains as the post-creation detail-completion flow. |
| Walker pickup status tracking (en_route / picked_up / dropped_off) | **Won't build — owner decision, July 2026.** The anticipatory `WalkEvent` model/table was never wired up (no writes, no recording UI) and was removed (migration `b40f4de664d4`, 2026-05-29). Remaining traces are deliberate permanent fossils: the `completed`/`modified` labels in the `booking_status` PG enum (never written; dropping an enum value needs a full table rewrite for zero gain) and the defensive `completed` exclusions in status filters / the partial unique index — leave all of them alone. |

---

## How to use this file

**Adding a request:** Add a row to the relevant section. Assign a `#`, estimate priority and effort.

**Effort guide:**
- **S (small):** Template tweak, new route, simple query — under an hour
- **M (medium):** New page/flow, moderate logic, touches 2-3 files — a few hours
- **L (large):** New subsystem, multiple routes/templates, external integrations — half a day+

**Reassessing:** Update priority/effort as we learn more. Move notes into the Notes column.
