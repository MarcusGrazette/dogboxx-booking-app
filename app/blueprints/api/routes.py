"""
API routes.

This module will define JSON API endpoints once the frontend needs them.
Currently a placeholder — the previous routes referenced non-existent model
attributes and have been removed during cleanup.
"""

from app.blueprints.api import api_bp

# TODO: Add API endpoints as needed, built against the actual models:
#   User, Client, Dog, DogOwner, Walker, WalkerSchedule,
#   ServiceType, Booking, BookingStatusChange, WalkEvent
