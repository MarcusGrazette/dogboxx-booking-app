[build]
builder = "nixpacks"

[deploy]
startCommand = "flask db upgrade && gunicorn run:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120 --log-level info"
healthcheckPath = "/auth/login"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
